<h1>Crudmuffin's Bikes</h1>

<ul>
  <% @bikes.each do |bike| %>
    <li>
      <strong> <%= bike.name %></strong>
      <strong><%= bike.year %></strong>
      <p><%= bike.description %></p>

      <%= link_to "Detail..", bike_path(bike) %>

    </li>
    <% end %>
    <br><br>
   <%= link_to "Add a new bike", new_bike_path %>
</ul>

